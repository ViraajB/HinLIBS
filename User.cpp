#include "User.h"

using namespace std;

//constructors that intilializses member variables with given args
User::User(const string& name, const string& type){
    userName = name;
    userType = type;
}

